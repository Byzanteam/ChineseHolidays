source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in chinese_holidays.gemspec
gemspec

group :test do
  gem 'pry-byebug'
  gem 'webmock'
end
